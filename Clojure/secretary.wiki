<div class="col-md-3 article-toc d-none d-md-block">
=Contents =
  - [[#secretary|secretary]]
    - [[#secretary#安装与引用|安装与引用]]
      - [[#secretary#安装与引用#依赖|依赖]]
      - [[#secretary#安装与引用#引用|引用]]
    - [[#secretary#基本使用|基本使用]]
      - [[#secretary#基本使用#html|html]]
      - [[#secretary#基本使用#源文件|源文件]]
        - [[#secretary#基本使用#源文件#设置显示井号|设置显示井号]]
        - [[#secretary#基本使用#源文件#定义路由|定义路由]]
        - [[#secretary#基本使用#源文件#设置默认地址（404）|设置默认地址（404）]]
        - [[#secretary#基本使用#源文件#配置历史|配置历史]]
        - [[#secretary#基本使用#源文件#设置首页|设置首页]]

</div>
<div class="col-md-9 article-main">
%template article_template
=secretary=
secretary是clojurescript的前端路由库，虽然三年没更新过，但是由于前端路由只是Google Closure Histroy的封装，对于一般的应用足够了，实在特殊的话就用直接用原生JS。
==安装与引用==
===依赖===
}}}
   [secretary "1.2.3"]
}}}
===引用===
{{{class="clojure"
(ns app.routes
  (:require [secretary.core :as secretary :refer-macros [defroute]]))

;; 如果需要用原生库
(ns app.routes 
  (:require [secretary.core :as secretary]
            [goog.events :as events])
  (:require-macros [secretary.core :refer [defroute]])
  (:import goog.History
           goog.history.EventType))

}}}

==基本使用==
===html===
在网页中只需要写出链接地址就可以，剩下的由路由解决
```html
	<a href="=/users/jim">Jim</a>=
}}}
===源文件===
====设置显示井号====
``` clojure
(secretary/set-config! :prefix "=")=

}}}

====定义路由====
要把每一个页面都进行一次定义，写出名字（可选）、绝对地址（可用正则表达式）、要传入的参数
和这个地址要执行的函数
{{{class="clojure"
;; /=/users/:id=
(defroute user-path "/users/:id" [id]
  (let [message (str "<h1>HELLO USER <small>" id "</small>!</h1>")]
    (set-html! application message)))
}}}

====设置默认地址（404）====
{{{class="clojure"
;; Catch all
(defroute "*" []
  (set-html! application "<h1>LOL! YOU LOST!</h1>"))
}}}

====配置历史====

{{{class="clojure"
;; Quick and dirty history configuration.
(let [h (History.)]
  (goog.events/listen h EventType.NAVIGATE =(secretary/dispatch! (.-token %)))=
  (doto h
    (.setEnabled true)))
}}}

====设置首页====
{{{class="clojure"
(secretary/dispatch! "/")
}}}
