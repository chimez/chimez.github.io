<div class="col-md-3 article-toc d-none d-md-block">
= Contents =
  - [[#cljs-ajax|cljs-ajax]]
    - [[#cljs-ajax#安装与引用|安装与引用]]
    - [[#cljs-ajax#基本使用|基本使用]]
      - [[#cljs-ajax#基本使用#消息处理|消息处理]]
      - [[#cljs-ajax#基本使用#基本GET|基本GET]]

</div>
<div class="col-md-9 article-main">
%template article_template
= cljs-ajax=
cljs-ajax为Clojure和ClojureScript提供相同Ajax的接口
== 安装与引用==
{{{
[cljs-ajax "0.7.3"]
(ns foo
  (:require [ajax.core :as ajax]))
	}}}
== 基本使用==
=== 消息处理===
在发送消息之后，反馈的消息由`handler`错误由`erroe-handler`捕获并处理，可以直接定义默认的处理函数，也可以为每个请求指定处理函数
消息处理函数只有一个参数
{{{
(defn handler [response]
  (.log js/console (str response)))

(defn error-handler [{:keys [status status-text]}]
  (.log js/console (str "something bad happened: " status " " status-text)))
	}}}
=== 基本GET===
注意地址的写法，本地地址要从`/`开始，不能省略
{{{
(ajax/GET "/hello")
(ajax/GET "/index.css" {:handler =(println %)})=
}}}
