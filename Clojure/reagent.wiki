<div class="col-md-3 article-toc d-none d-md-block">
= Contents =
  - [[#reagent|reagent]]
    - [[#reagent#安装与配置|安装与配置]]
    - [[#reagent#基本语法|基本语法]]
      - [[#reagent#基本语法#组件|组件]]
        - [[#reagent#基本语法#组件#组件的定义|组件的定义]]
        - [[#reagent#基本语法#组件#组件的调用和传参|组件的调用和传参]]
        - [[#reagent#基本语法#组件#可以用的标签|可以用的标签]]
      - [[#reagent#基本语法#DOM渲染|DOM渲染]]
        - [[#reagent#基本语法#DOM渲染#render函数|render函数]]
        - [[#reagent#基本语法#DOM渲染#获取元素的方法|获取元素的方法]]
      - [[#reagent#基本语法#页面状态|页面状态]]
        - [[#reagent#基本语法#页面状态#reagent.core/atom|reagent.core/atom]]
        - [[#reagent#基本语法#页面状态#初次定义时进行设置|初次定义时进行设置]]
        - [[#reagent#基本语法#页面状态#使用React生命循环反馈|使用React生命循环反馈]]

</div>
<div class="col-md-9 article-main">
%template article_template
= reagent=
reagent是clojurescript上对reagent的一个封装，使用了类似hiccup的语法
== 安装与配置==
1. 在shadow-cljs配置中加入`[reagent "0.8.0-alpha2"]`
2. 引用`(ns app.core (:require [reagent.core :as r]))`

== 基本语法==
=== 组件===
==== 组件的定义====
组件在形式上就是一个函数,内部是用方括号嵌套表示的html标签
{{{class="clojure"
(defn some-component []
  [:div
   [:h3 "I am a component!"]
   [:p.someclass 
    "I have " [:strong "bold"]
    [:span {:style {:color "red"}} " and red"]
    " text."]])
}}}
嵌套标签可以快捷表示
{{{class="clojure"
[:div>p>b "Nested Element"]
}}}
class也可以嵌套表示
{{{class="clojure"
[:div {:class ["a-class" (when active? "active") "b-class"]}]
}}}

==== 组件的调用和传参====
由于组件就是个函数，所以可以像使用函数那样传递参数，不过用中括号而不是小括号
{{{class="clojure"
(defn child [name]
  [:p "Hi, I am " name])

(defn childcaller []
  [child "Foo Bar"])
}}}

==== 可以用的标签====
|标签||
|`:div`||
|`:h1`||
|`:p`||
|`:strong`||
|`:span`||
|`:style`||
|`:class`||

=== DOM渲染===
==== render函数====
使用`reagent/render`函数，第一个参数是调用组件`[component]`,第二个参数是DOM元素
{{{class="clojure"
(defn mountit []
  (r/render [childcaller]
            (.-body js/document)))
(defn ^:export main [] ) ;使用export可以将函数暴漏出去，可以在页面的js里调用
}}}

==== 获取元素的方法====
使用js的document函数
{{{class="clojure"
(.-body js/document)  
(.getElementById js/document "app")
}}}

=== 页面状态===
==== reagent.core/atom====
使用reagent版的atom表示页面状态
{{{class="clojure"
(defonce click-count (r/atom 0)) ;定义一个atom

(defn state-ful-with-atom []
  [:div {:on-click =(swap! click-count inc)}  ;在组件中对atom的值进行更改=
   "I have been clicked " @click-count " times."]) ;引用atom的值

}}}
==== 初次定义时进行设置====
{{{class="clojure"
(defn timer-component []
  (let [seconds-elapsed (r/atom 0)]
    (fn []
      (js/setTimeout =(swap! seconds-elapsed inc) 1000)=
      [:div
       "Seconds Elapsed: " @seconds-elapsed])))

}}}

==== 使用React生命循环反馈====
{{{class="clojure"
(defonce my-html (r/atom ""))

(defn plain-component []
  [:p "My html is " @my-html])

(def component-with-callback
  (with-meta plain-component
    {:component-did-mount
     (fn [this]
       (reset! my-html (.-innerHTML (reagent/dom-node this))))}))
}}}
