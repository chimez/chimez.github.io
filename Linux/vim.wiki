<div class="col-md-3 article-toc d-none d-md-block">
= Contents =
  - [[#vim操作记录|vim操作记录]]
  - [[#编辑模式|编辑模式]]
    - [[#编辑模式#光标移动|光标移动]]
      - [[#编辑模式#光标移动#基础移动|基础移动]]
      - [[#编辑模式#光标移动#单词移动|单词移动]]
      - [[#编辑模式#光标移动#页定位|页定位]]
      - [[#编辑模式#光标移动#行内移动|行内移动]]
    - [[#编辑模式#搜索|搜索]]
    - [[#编辑模式#删除|删除]]
    - [[#编辑模式#替换|替换]]
    - [[#编辑模式#复制粘贴|复制粘贴]]
      - [[#编辑模式#复制粘贴#寄存器|寄存器]]
      - [[#编辑模式#复制粘贴#粘贴，从缓冲区输出内容|粘贴，从缓冲区输出内容]]
      - [[#编辑模式#复制粘贴#复制，将内容读入缓冲区|复制，将内容读入缓冲区]]
  - [[#撤销和重复|撤销和重复]]
  - [[#插入|插入]]
  - [[#命令模式|命令模式]]
    - [[#命令模式#打开、保存与退出|打开、保存与退出]]
    - [[#命令模式#行定位|行定位]]
    - [[#命令模式#正则表达式，搜索与替换|正则表达式，搜索与替换]]
      - [[#命令模式#正则表达式，搜索与替换#字符含义|字符含义]]
      - [[#命令模式#正则表达式，搜索与替换#指令|指令]]
    - [[#命令模式#恢复文件|恢复文件]]
    - [[#命令模式#执行shell命令|执行shell命令]]
    - [[#命令模式#录制宏|录制宏]]
    - [[#命令模式#分屏与标签页|分屏与标签页]]
      - [[#命令模式#分屏与标签页#分屏|分屏]]
        - [[#命令模式#分屏与标签页#分屏#开启|开启]]
        - [[#命令模式#分屏与标签页#分屏#切换|切换]]
        - [[#命令模式#分屏与标签页#分屏#调整|调整]]
      - [[#命令模式#分屏与标签页#标签页|标签页]]
  - [[#插件与设置|插件与设置]]
    - [[#插件与设置#设置选项|设置选项]]
    - [[#插件与设置#插件|插件]]
      - [[#插件与设置#插件#配置管理器|配置管理器]]
        - [[#插件与设置#插件#配置管理器#vim-plug 安装步骤|vim-plug 安装步骤]]

</div>
<div class="col-md-9 article-main">
%template article_template
=vim操作记录 =
=编辑模式=
==光标移动==
===基础移动===
| 指令     | 作用                                   |
| `k`      | 上移；                                 |
| `j`      | 下移；                                 |
| `h`      | 左移；                                 |
| `l`      | 右移。                                 |
| `ctrl+f` | 在文件中前移一页（相当于 page down）； |
| `ctrl+b` | 在文件中后移一页（相当于 page up）；   |

===单词移动===
| 指令      | 作用                                                               |
| `*`       | 当光标停留在一个单词上，* 键会在文件内搜索该单词，并跳转到下一处； |
| `=`       | 当光标停留在一个单词上，=在文件内搜索该单词，并跳转到上一处；==    |
| `(/)`     | 移动到 前/后 句 的开始；                                           |
| `{/}`     | 跳转到 当前/下一个 段落 的开始。                                   |
| `g_`      | 到本行最后一个不是 blank 字符的位置。                              |
| `fa`      | 到下一个为 a 的字符处，你也可以fs到下一个为s的字符。               |
| `t,`      | 到逗号前的第一个字符。逗号可以变成其它字符。                       |
| `3fa`     | 在当前行查找第三个出现的 a。                                       |
| `F/T`     | 和 f 和 t 一样，只不过是相反方向;                                  |
| `gg`      | 将光标定位到文件第一行起始位置；                                   |
| `G`       | 将光标定位到文件最后一行起始位置；                                 |
| `NG或Ngg` | 将光标定位到第 N 行的起始位置。                                    |

===页定位===
| 指令 | 作用                                   |
| `H`  | 将光标移到屏幕上的起始行（或最上行）； |
| `M`  | 将光标移到屏幕中间；                   |
| `L`  | 将光标移到屏幕最后一行。               |

===行内移动===
| 指令 | 作用                               |
| `w`  | 右移光标到下一个字的开头；         |
| `e`  | 右移光标到一个字的末尾；           |
| `b`  | 左移光标到前一个字的开头；         |
| `0`  | 数字０，左移光标到本行的开始；     |
| `$`  | 右移光标，到本行的末尾；           |
| `^`  | 移动光标，到本行的第一个非空字符。 |

==搜索==
| 指令    | 作用                                         |
| `/str1` | 正向搜索字符串 str1；                        |
| `n`     | 继续搜索，找出 str1 字符串下次出现的位置；   |
| `N`     | 继续搜索，找出 str1 字符串上一次出现的位置； |
| `?str2` | 反向搜索字符串 str2 。                       |
| `:noh`  | 取消搜索高亮                                 |

==删除==
| 指令 | 作用                                     |
| `r`  | 替换光标所指向的当前字符；               |
| `x`  | 删除光标所指向的当前字符；               |
| `dw` | 删除光标右侧的字；                       |
| `db` | 删除光标左侧的字；                       |
| `dd` | 删除光标所在行，并去除空隙；             |
| `d$` | 从当前光标起删除字符直到行的结束；       |
| `d0` | 从当前光标起删除字符直到行的开始；       |
| `J`  | 删除本行的回车符（CR），并和下一行合并。 |

==替换==
| 指令 | 作用                                              |
| `s`  | 用输入的正文替换光标所指向的字符；                |
| `S`  | 删除当前行，并进入编辑模式；                      |
| `cw` | 用输入的正文替换光标右侧的字；                    |
| `cW` | 用输入的正文替换从光标到行尾的所有字符（同 c$ )； |
| `cb` | 用输入的正文替换光标左侧的字；                    |
| `cd` | 用输入的正文替换光标的所在行；                    |
| `c$` | 用输入的正文替换从光标开始到本行末尾的所有字符；  |
| `c0` | 用输入的正文替换从本行开头到光标的所有字符。      |

==复制粘贴==
===寄存器===

| 指令    | 作用                          |
| `:reg`  | 显示查看寄存器内容            |
| `"ap`   | 粘贴字母a寄存器内容           |
| `"1y`   | 把选取内容复制到数字寄存器1   |
| `"+y`   | 复制到系统剪贴板              |
| `"+p`   | 粘贴                          |
| `"+gp`  | 粘贴并且移动光标到粘贴内容后  |
| `C-r 1` | 插入寄存器内容,在命令行中可用 |
===粘贴，从缓冲区输出内容===
| 指令 | 作用                                         |
| `p`  | 小写字母 p，将缓冲区的内容粘贴到光标的后面； |
| `P`  | 大写字母 P，将缓冲区的内容粘贴到光标的前面。 |

===复制，将内容读入缓冲区===
| 指令    | 作用                          |
| `yy`    | 复制当前行到内存缓冲区；      |
| `:+y`   | 复制 1 行到操作系统的粘贴板； |
| `:+nyy` | 复制 n 行到操作系统的粘贴板。 |

=撤销和重复=
| 指令 | 作用                         |
| `u`  | 撤消前一条命令的结果；       |
| `.`  | 重复最后一条修改正文的命令。 |

=插入=
| 指令     | 作用                         |
| `i`      | 在光标左侧插入正文           |
| `a`      | 在光标右侧插入正文           |
| `o`      | 在光标所在行的下一行增添新行 |
| `O`      | 在光标所在行的上一行增添新行 |
| `I`      | 在光标所在行的开头插入       |
| `A`      | 在光标所在行的末尾插入       |
| `ESC`    | 退出                         |
| `Ctrl+[` | 退出                         |

=命令模式=
==打开、保存与退出==
| 指令                       | 作用             |
| `:e` path_to_file/filename | 打开文件         |
| `:w`                       | 保存             |
| `:q`                       | 退出             |
| `！`                       | 强制执行         |
| `:r`                       | 读取文件         |
| `:f`                       | 重命名与文件相关 |

==行定位==
| 指令                         | 作用                                             |
| `:number,number2	something` | 直接输入数字指定行号，输入逗号和两个数字表示范围 |

==正则表达式，搜索与替换==
===字符含义===
| 指令     | 作用                              |
| `^`      | 放在字符串前面，匹配行首的字；    |
| `$`      | 放在字符串后面，匹配行尾的字；    |
| `\<`     | 匹配一个字的字头；                |
| `\>`     | 匹配一个字的字尾；                |
| `.`      | 匹配任何单个正文字符；            |
| `[str]`  | 匹配 str 中的任何单个字符；       |
| `[^str]` | 匹配任何不在 str 中的单个字符；   |
| `[a-b]`  | 匹配 a 到 b 之间的任一字符；      |
| `*`      | 匹配前一个字符的 0 次或多次出现； |
| `\`      | 转义后面的字符。                  |

===指令===
| 指令 | 作用     |
| `s`  | 替换     |
| `g`  | 全部执行 |
| `%`  | 所有行   |
| `d`  | 删除     |

==恢复文件==
| 指令         | 作用     |
| `file.swp`   | 缓存文件 |
| ``:recover`` | 恢复     |

==执行shell命令==
| 指令          | 作用     |
| `:!something` | 执行命令 |

==录制宏==
| 指令 | 作用     |
| `qa` | 开始录制 |
| `q`  | 结束     |
| `@a` | 调用宏   |
* a表示寄存器编号，可以有b、c、d
* 想在命令里使用宏的话，用`:normal @a`，可以执行命令

==分屏与标签页==
===分屏===
====开启====
| 指令                       | 作用       |
| `:split（可用缩写 :sp）`   | 上下分屏； |
| `:sv`                      | 上下       |
| `:vsplit（可用缩写 :vsp）` | 左右分屏。 |
| `:vs`                      | 左右       |

====切换====
| 指令       | 作用                       |
| `Ctrl+w+h` | 切换到当前分屏的左边一屏； |
| `Ctrl+w+l` | 切换到当前分屏的右边一屏； |
| `Ctrl+w+j` | 切换到当前分屏的下方一屏； |
| `Ctrl+w+k` | 切换到当前分屏的上方一屏。 |
| `Ctrl+w+w` | 循环切换                   |

====调整====
| 指令       | 作用                       |
| `Ctrl+W =` | 让所有的屏都有一样的高度； |
| `Ctrl+W +` | 增加高度；                 |
| `Ctrl+W -` | 减少高度。                 |
| `Ctrl+w <` | 宽度变化                   |
| `ctrl+w >` | 宽度变化                   |

===标签页===
| 指令                 | 作用                                     |
| `:tabnew filename`   | 打开新标签页                             |
| `:tN[ext]`           | 跳转到上一个匹配的标签                   |
| `:tabN[ext]`         | 跳到上一个标签页                         |
| `:tabc[lose]`        | 关闭当前标签页                           |
| `:tabdo`             | 为每个标签页执行命令                     |
| `:tabe[dit]`         | 在新标签页里编辑文件                     |
| `:tabf[ind]`         | 寻找 'path' 里的文件，在新标签页里编辑之 |
| `:tabfir[st]`        | 转到第一个标签页                         |
| `:tabl[ast]`         | 转到最后一个标签页                       |
| `:tabm[ove] N`       | 把标签页移到序号为N位置                  |
| `:tabnew [filename]` | 在新标签页里编辑文件                     |
| `:tabn[ext]`         | 转到下一个标签页                         |
| `:tabo[nly]`         | 关闭所有除了当前标签页以外的所有标签页   |
| `:tabp[revious]`     | 转到前一个标签页                         |
| `:tabr[ewind]`       | 转到第一个标签页                         |


=插件与设置=
==设置选项==
| 指令          | 作用                                                         |
| `:set option` | 设置选项 option                                              |
| `autoindent`  | 设置该选项，则正文自动缩进                                   |
| `ignorecase`  | 设置该选项，则忽略规则表达式中大小写字母的区别               |
| `number`      | 设置该选项，则显示正文行号                                   |
| `ruler`       | 设置该选项，则在屏幕底部显示光标所在行、列的位置             |
| `tabstop`     | 设置按 Tab 键跳过的空格数。例如 :set tabstop=n，n 默认值为 8 |
| `mk`          | 将选项保存在当前目录的 .exrc 文件中                          |

==插件==
===配置管理器===
传统Vundle	https://github.com/VundleVim/Vundle.vim
vim-plug		https://github.com/junegunn/vim-plug
====vim-plug 安装步骤====
1. 安装git  https://git-for-windows.github.io/
2. 下载plug.vim 到 ~\vimfiles\autoload\plug.vim
3. 配置.vimrc	添加 call plug=begin() -- Plug 'something' -- call plug=end()==
4. Reload .vimrc and :PlugInstall to install plugins.
